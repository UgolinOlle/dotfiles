# --- Applications
cmd + alt - a : open -a "Dia"
cmd + alt - m : open -a "Mail"
cmd + alt - y : open -a "Spotify"
cmd + alt - n : open -a "Notion"
cmd + alt - d : open -a "Discord"
cmd + alt - p : open -a "TablePlus"
cmd + alt - h : open -a "HTTPie"
cmd + alt - z : open -a "Zed"
cmd + alt - x : open -a "CleanMyMac"
cmd + alt - f : open -a "Figma"
cmd + alt - s : open -a "Screen Studio"
cmd + alt - l : open -a "Slack"
cmd + alt - t : open -a "Telegram"
cmd + alt - w : open -a "WhatsApp"

alt - return : open -a "Wezterm"
alt - f : open ~ 

# --- Reload config files
alt - r : skhd --reload
