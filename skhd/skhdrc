# --- Applications
cmd + alt - s : open -a "Arc"
cmd + alt - m : open -a "Mail"
cmd + alt - c : open -a "Notion Calendar"
cmd + alt - s : open -a "Spotify"
cmd + alt - n : open -a "Notion"
cmd + alt - d : open -a "Discord"
cmd + alt - o : open -a "OrbStack"
cmd + alt - t : open -a "TablePlus"
cmd + alt - h : open -a "HTTPie"
cmd + alt - v : open -a "Cursor"
cmd + alt - x : open -a "CleanMyMac"
cmd + alt - z : open -a "Zoom"
alt - return : open -a "Wezterm"
alt - f : open ~ 

# --- Reload config files
alt - r : skhd --reload
