# --- Applications
cmd + alt - d : open -a "Dia"
cmd + alt - m : open -a "Mail"
cmd + alt - s : open -a "Spotify"
cmd + alt - n : open -a "Notion"
cmd + alt - o : open -a "Discord"
cmd + alt - t : open -a "TablePlus"
cmd + alt - h : open -a "HTTPie"
cmd + alt - z : open -a "Zed"
cmd + alt - x : open -a "CleanMyMac"
cmd + alt - f : open -a "Figma"
cmd + alt - b : open -a "Beeper Desktop"
cmd + alt - a : open -a "Activity Monitor"
cmd + alt - k : open -a "Docker"

alt - return : open -a "Ghostty"
alt - f : open ~ 

# --- Reload config files
alt - r : skhd --reload
